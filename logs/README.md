# Logs Directory

This directory contains debug logs from the Claude Code Agent node executions.

Log files are named `debug-<timestamp>.log` and include:
- Configuration details
- Tool schema information
- SDK message stream
- Error details and stack traces

Logs are automatically created when the node executes with verbose mode enabled.
